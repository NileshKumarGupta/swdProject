departmentSemester = {
    departmentName: "Computer Science",
    year: "2",
    semester: "1",
};

// refresh timeTable when something is pressed
// make a function to show the courses of department after selection of the course

assignFunctions();
getData();
createInitialConfiguration(sectionList);